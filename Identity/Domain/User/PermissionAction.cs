namespace Domain.User
{
    public enum PermissionAction
    {
        Add = 0,        // Represents the action of adding something
        View = 1,       // Represents the action of viewing something
        Change = 2,     // Represents the action of changing (editing) something
        Delete = 3      // Represents the action of deleting something
    }
}